# --- Required ---
# Absolute path to the local MedGemma GGUF model file
MEDGEMMA_GGUF_PATH=/absolute/path/to/models/medgemma-27b-it-Q3_K_M.gguf

# --- Performance Tuning (Highly Recommended) ---
# Controls how much of the model is offloaded to your GPU (Metal).
# -1 means offload all layers to GPU (fastest).
MEDGEMMA_GPU_LAYERS=-1
# CPU threads to use (0 means auto-detect).
MEDGEMMA_N_THREADS=0

# --- Behavior Overrides (Optional) ---
# The maximum tokens the model is allowed to generate per response by default.
MEDGEMMA_MAX_NEW_TOKENS=512
# Timeout for the background clinical data extraction.
MEDGEMMA_EXTRACTION_FINAL_TIMEOUT_S=5
# Timeout for final auto-report generation after END_SESSION.
MEDGEMMA_REPORT_AUTO_TIMEOUT_S=90
# Heartbeat cadence for websocket `report_status=running` events while report is generating.
MEDGEMMA_REPORT_STATUS_HEARTBEAT_S=2
# Max time an HTTP /report call waits to acquire the shared LLM lock before returning LLM_BUSY.
MEDGEMMA_REPORT_HTTP_LOCK_TIMEOUT_S=3
# Uvicorn websocket keepalive tuning (prevents premature 1011 disconnects on long turns).
MEDGEMMA_WS_PING_INTERVAL_S=60
MEDGEMMA_WS_PING_TIMEOUT_S=120
